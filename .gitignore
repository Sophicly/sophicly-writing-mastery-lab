# macOS
.DS_Store

# Claude Code local settings
.claude/settings.local.json

# Session handoffs (working documents, not source code)
session-handoff-*.md
SESSION_HANDOFF_*.md

# Research/reference docs (large, not source code)
exam-paper-structures*.md
question-bank-*.md

# Misc
*.html

# Screenshots (CleanShot auto-saves land here when shared in chat)
CleanShot*.png
CleanShot*.jpg
.agents/
.claude/skills/

# Claude Code session permissions — machine/session cruft, can capture secrets; never commit
.claude/settings.json
.claude/settings.local.json

# Step 9 island build deps (the built bundle in frontend/ is committed; deps are not)
island/node_modules/
island/package-lock.json
protocols/_marks/.pdftext-cache/
